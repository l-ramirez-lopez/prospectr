# get the metadata of the samples in the nircal file

internal

## Usage

``` r
get_nircal_metadata(
  connection,
  n,
  spctra_start,
  spcinfo,
  progress,
  pb,
  progress.start,
  progress.steps
)
```
